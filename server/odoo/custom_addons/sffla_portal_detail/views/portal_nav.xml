<odoo>
    <template id="portal_nav" name="Portal Navbar">
        <ul class="nav nav-tabs mb-4 justify-content-center shadow-sm rounded bg-light p-2">

            <!-- Partner -->
            <li class="nav-item mx-2">
                <a t-att-class="'nav-link rounded ' + ('bg-primary text-white' if active_page == 'partner' else 'text-dark')"
                   href="/my/partner">
                    Partner Detail
                </a>
            </li>

            <!-- Kastam -->
            <li class="nav-item mx-2">
                <a t-att-class="'nav-link rounded ' + ('bg-warning text-dark' if active_page == 'kastam' else 'text-dark')"
                   href="/my/kastam">
                    Kastam Detail
                </a>
            </li>

            <!-- Services -->
            <li class="nav-item mx-2">
                <a t-att-class="'nav-link rounded ' + ('bg-success text-white' if active_page == 'services' else 'text-dark')"
                   href="/my/services">
                    Services
                </a>
            </li>

            <!-- SSM -->
            <li class="nav-item mx-2">
                <a t-att-class="'nav-link rounded ' + ('bg-info text-white' if active_page == 'ssm' else 'text-dark')"
                   href="/my/ssm">
                    SSM Detail
                </a>
            </li>

            <!-- Membership -->
            <li class="nav-item mx-2">
                <a t-att-class="'nav-link rounded ' + ('bg-danger text-white' if active_page == 'subscription' else 'text-dark')"
                   href="/my/membership">
                    Membership Detail
                </a>
            </li>
        </ul>
    </template>
</odoo>
